The config.yaml file serves as the layout specification for the Shaders.
Used to Define :-
    Audio objects
    Shader objects, belonging to an Audio object

An Audio object represents various Audio Capturing and transformation properties
Contains:
    Audio Name : Name of the Audio Capturing thread
    Pipewire Settings : Represents Audio Capturing properties, such as Sample Rate and Buffer Size 
    Audio Overrides : Represents Audio Transformation properties, such as minimum/maximum frequency, smoothing parameters, and averaging / gravity parameters
    Shader Objects : Represents the list of Shaders that take in as input the transformed Audio values for visualisation

A Shader Object represents the layout properties of the Shader Program that gets displayed in a Window.
Contains:
    Shader Name: Name of the Shader Program to display
    Class Name: Class Name to identify the Shader for Styling purposes
    FPS: Target Framerate
    Width, Height: Bounds of the Window
    layer:  the placement of the Shader on the 'Z Axis' on the screen, via the Wayland Layer Shell Protocol. Supported Types are Background, Bottom, Top, and Overlay
    anchors: Edges to anchor to
    margins: Margin from anchored Edges
    Overrides: GLSL Overrides for specifying different `#define` attribute substitutions
    Audio Overrides: Audio Overrides that deviate from the 'Global' Audio Overrides (that is, Overrides that belong to the parent Audio Object for the current Shader)
    Paintables: List of 'Copies' of the Current Shader that can be displayed in another Window

A Paintable Object represents the layout properties for the Paintable
Contains:
    Class Name: Class Name to identify the Shader for Styling purposes
    Width, Height: Bounds of the Window
    layer: Specifies the placement of the Shader on the 'Z Axis' on the screen, via the Wayland Layer Shell Protocol. Supported Types are Background, Bottom, Top, and Overlay
    anchors: Edges to anchor to
    margins: Margin from anchored Edges

config.yaml therefore has the following structure:

audios:
    - name: audio1
      pipewire-settings:
        .
        .
        .
      audio-overrides:
        .
        .
        .
      shaders:
        - name: linear
          class-name: linear1
          .
          .
          .
          audio-overrides:
            .
            .
            .
          paintables:
            - class-name: paintable_1
              .
              .
              .
          overrides:
            - visualiserMode=1
            .
            .
            .
    - name: audio2
    .
    .
    .

